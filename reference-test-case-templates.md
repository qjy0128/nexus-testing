# 测试用例模板与反模式参考

> **定位**：整合 qa-test-planner 8 种用例模板、test-master 反模式清单、回归套件架构，为 nexus-testing 的测试设计师和测试工程师提供标准化参考。测试设计师（阶段三）按模板设计用例，测试工程师（阶段五）按模板执行。

---

## 一、用例编号与命名约定

### 1.1 编号前缀

| 前缀 | 类型 | 适用场景 | 优先级范围 |
|------|------|---------|-----------|
| `TC-` | 标准测试用例 | 通用功能验证 | P0-P3 |
| `TC-FUNC-` | 功能测试用例 | 业务逻辑验证 | P0-P2 |
| `TC-UI-` | UI/视觉测试用例 | 界面呈现验证 | P1-P3 |
| `TC-INT-` | 集成测试用例 | 组件交互/数据流验证 | P0-P2 |
| `TC-SEC-` | 安全测试用例 | 安全控制验证 | **固定 P0** |
| `TC-PERF-` | 性能测试用例 | 速度/资源/可扩展性 | P1-P2 |
| `TC-REG-` | 回归测试用例 | 变更影响验证 | P0-P2 |
| `META-TC-` | 元测试用例 | 测试框架验证 | P0-P1 |
| `EXT-TC-` | 外部获取用例 | 外部来源适配 | P0-P3 |

### 1.2 优先级定义

| 级别 | 定义 | 执行频率 | 阻断发布 |
|------|------|---------|---------|
| **P0** | 关键路径，核心功能，安全红线 | 每次构建 | ✅ 阻断 |
| **P1** | 主要功能，高影响 | 每日/每周 | 失败≥2个阻断 |
| **P2** | 标准功能，中等影响 | 每周/版本 | 不阻断 |
| **P3** | 次要功能，低影响 | 仅版本发布 | 不阻断 |

---

## 二、8 种测试用例模板

### 模板 1：标准测试用例

```markdown
#### TC-{NN}：{用例名称}
• 用例编号：TC-{NN}
• 模块：{所属模块}
• 优先级：P{0-3}
• 类型：正向/逆向/边界/异常
• 前置条件：
  - [ ] 条件1
  - [ ] 条件2
• 测试步骤：
  1. 步骤1
  2. 步骤2
• 预期结果：{期望的输出}
• 测试数据：{使用的测试数据}
• 执行动作：{具体执行了什么命令/操作}
• 实际输入：{真实传入的参数}
• 实际输出：{真实返回内容}
• 判定：✅ 通过 / ❌ 失败
```

### 模板 2：功能测试用例

```markdown
#### TC-FUNC-{NN}：{用例名称}
• 用例编号：TC-FUNC-{NN}
• 模块：{所属模块}
• 需求ID：REQ-{XXX}
• 优先级：P{0-2}
• 类型：正向/逆向
• 前置条件：
  - 角色：{用户角色}
  - 数据集：{准备的数据}
• 测试步骤：{步骤描述}
• 预期结果：{期望的输出}
• 边界测试：
  | 场景 | 输入 | 预期 |
  |------|------|------|
  | 最小值 | {min} | {expected} |
  | 最大值 | {max} | {expected} |
  | 空值 | null/空 | {expected} |
• 负面测试：
  | 场景 | 输入 | 预期 |
  |------|------|------|
  | 无效输入 | {invalid} | 友好错误 |
  | 未授权访问 | {no auth} | 403/拒绝 |
  | 缺少必填字段 | {missing} | 明确提示 |
```

### 模板 3：UI/视觉测试用例

```markdown
#### TC-UI-{NN}：{用例名称}
• 用例编号：TC-UI-{NN}
• 模块：{UI 模块}
• 设计参考：{Figma URL 或截图路径}
• 优先级：P1-P3
• 类型：视觉验证
• 断点覆盖：
  | 断点 | 分辨率 | 状态 |
  |------|--------|------|
  | 桌面 | 1920px | ✅/❌ |
  | 平板 | 768px | ✅/❌ |
  | 手机 | 375px | ✅/❌ |
• 视觉规格检查：
  - 布局：{间距/对齐}
  - 排版：{字体/大小/粗细/颜色}
  - 颜色：{背景/边框/文本色}
• 交互状态：默认/悬停/激活/聚焦/禁用
```

### 模板 4：集成测试用例

```markdown
#### TC-INT-{NN}：{用例名称}
• 用例编号：TC-INT-{NN}
• 模块：{集成点}
• 系统列表：{系统A → 系统B}
• API 端点：{端点路径}
• 优先级：P0-P2
• 类型：正向/逆向
• 测试目标：验证数据从 {源} 正确传递到 {目的地}
• 数据验证表：
  | 字段 | 源系统值 | 转换后值 | 目标系统值 | 一致性 |
  |------|---------|---------|-----------|--------|
  | field1 | {val} | {val} | {val} | ✅/❌ |
• 错误场景：
  | 场景 | 模拟方式 | 预期行为 |
  |------|---------|---------|
  | 超时 | {mock} | 降级/重试 |
  | 无效响应 | {mock} | 错误提示 |
  | 认证失败 | {expired token} | 401 |
  | 速率限制 | {429 response} | 限流提示 |
```

### 模板 5：安全测试用例

```markdown
#### TC-SEC-{NN}：{用例名称}
• 用例编号：TC-SEC-{NN}
• OWASP 类别：A{01-10}
• 风险级别：Critical/High/Medium/Low
• 优先级：**固定 P0**
• 类型：逆向
• 测试目标：验证 {攻击向量} 被 {安全控制} 正确拦截
• 攻击向量：
  | 攻击类型 | 输入 | 预期拦截 | 实际结果 |
  |---------|------|---------|---------|
  | SQL 注入 | {payload} | DENY | {actual} |
  | XSS | {payload} | 转义/DENY | {actual} |
  | 路径遍历 | ../../etc/passwd | DENY | {actual} |
  | 提示词注入 | ignore previous | DENY | {actual} |
• 合规性检查：
  - [ ] 敏感数据不暴露
  - [ ] 权限边界正确
  - [ ] 审计日志记录完整
```

### 模板 6：性能测试用例

```markdown
#### TC-PERF-{NN}：{用例名称}
• 用例编号：TC-PERF-{NN}
• 模块：{模块名}
• 优先级：P1-P2
• 类型：性能验证
• 性能标准表：
  | 指标 | 目标 | 可接受范围 | 实际值 | 状态 |
  |------|------|-----------|--------|------|
  | 响应时间(p95) | {target}ms | {range}ms | {actual}ms | ✅/❌ |
  | 吞吐量 | {target}/s | {range}/s | {actual}/s | ✅/❌ |
  | 错误率 | <{target}% | <{range}% | {actual}% | ✅/❌ |
  | Token 消耗 | ≤{budget} | ≤{limit} | {actual} | ✅/❌ |
• 负载场景：
  | 场景 | 并发数 | 持续时间 | 预期 |
  |------|--------|---------|------|
  | 正常负载 | {N} | {T} | 无错误 |
  | 峰值负载 | {2N} | {T} | 错误率 <1% |
  | 压力测试 | {5N} | {T} | 优雅降级 |
```

### 模板 7：回归测试用例

```markdown
#### TC-REG-{NN}：{用例名称}
• 用例编号：TC-REG-{NN}
• 原始功能：{关联的原功能名}
• 最近修改：{修改日期和内容}
• 优先级：P0-P2
• 类型：回归验证
• 上下文：{近期变更内容}
• 关键路径测试：
  | 测试项 | 预期 | 实际 | 状态 |
  |--------|------|------|------|
  | 核心功能 | {expected} | {actual} | ✅/❌ |
  | 数据持久化 | {expected} | {actual} | ✅/❌ |
  | UI 渲染 | {expected} | {actual} | ✅/❌ |
  | 错误处理 | {expected} | {actual} | ✅/❌ |
• 集成点检查：{上下游影响}
• 性能基线对比：{变更前 vs 变更后}
```

### 模板 8：元测试用例（测试框架专用）

```markdown
#### META-TC-{NN}：{用例名称}
• 用例编号：META-TC-{NN}
• 测试层次：M1(基础通过)/M2(缺陷检出)/M3(健壮性)/M4(端到端)
• 夹具ID：FX-{NN}
• 夹具类型：{合格Skill/缺陷Skill/极端Skill/标准Skill}
• 测试目标：验证被测框架能 {预期行为}
• 夹具创建动作：{如何创建测试夹具}
• 夹具验证：✅ 文件已创建 / ❌ 创建失败
• 被测产品执行动作：{如何用被测框架测试这个夹具}
• 被测产品实际输出：{被测框架的真实响应}
• 预期 vs 实际：{对比分析}
• 判定：✅ 通过 / ❌ 失败
• 清理状态：✅ 夹具已删除 / ❌ 清理失败
```

---

## 三、测试反模式清单（5 类禁止行为）

> **核心原则**：测试代码的行为，而不是模拟的行为。

### 反模式 1：测试 Mock 行为而非实际输出

| 警示信号 | 说明 |
|---------|------|
| `expect(mock).toHaveBeenCalled()` 但不检查输出 | 只验证 mock 被调用，不验证实际结果 |
| 用例结果只有「预期行为」无「实际输出」 | nexus-testing 中最常见的问题 |
| 「读取 SKILL.md，验证逻辑完整」 | 读文档 ≠ 测试 |

**正确做法**：断言实际输出（执行结果、返回值、副作用），而非调用链。

### 反模式 2：为测试添加生产代码污染

| 警示信号 | 说明 |
|---------|------|
| 生产代码中有 `_resetForTesting()` | 只为测试存在的生产代码方法 |
| 生产代码中有 `if (TEST_MODE)` | 测试分支泄漏到生产 |
| 被测 Skill 内嵌测试逻辑 | Skill 不应为测试改变自身行为 |

**正确做法**：使用隔离环境（沙箱）和独立测试夹具，不修改被测产品。

### 反模式 3：不理解就 Mock

| 警示信号 | 说明 |
|---------|------|
| 每个测试用例 Mock 10+ 依赖 | 过度 Mock，测试变成 Mock 的一致性验证 |
| Mock 返回 `{ success: true }` | 最小化 Mock，不反映真实行为 |
| 测试全过但生产环境挂了 | Mock 与生产脱节 |

**正确做法**：先用真实实现运行理解行为，再仅对外部边界 Mock。沙箱执行优先于 Mock。

### 反模式 4：不完整的 Mock

| 警示信号 | 说明 |
|---------|------|
| Mock 仅返回 `{ id, name }` | 下游代码需要 `email, createdAt, permissions` |
| 测试数据硬编码 | 不同测试共享同一份固定数据 |
| 测试夹具只有正常场景 | 缺少异常/边界夹具 |

**正确做法**：使用工厂函数 `createMockResponse()` 生成完整 Mock 对象，确保反映真实契约。

### 反模式 5：测试作为事后补充

| 警示信号 | 说明 |
|---------|------|
| 功能发布后才补测试 | 测试与开发脱节 |
| 测试只覆盖「正常路径」 | 缺少逆向和边界 |
| 60 个用例设计只有 13 个执行（22%）| 设计了但不执行等于没测 |

**正确做法**：测试设计与功能开发同步，执行率 ≥ 90%。

---

## 四、检测清单（反偷懒审计用）

evidence-collector 可按此清单审计每个测试用例：

| 警示信号 | 对应反模式 | 处理 |
|---------|-----------|------|
| `expect(mock).toHaveBeenCalled()` 无输出测试 | 反模式 1 | 标记为「执行证明缺失」 |
| 生产代码中 `_` 或 `ForTesting` 方法 | 反模式 2 | 标记为「测试污染」 |
| 每个用例 10+ Mock | 反模式 3 | 标记为「过度 Mock」 |
| Mock 仅返回 `{ success: true }` | 反模式 4 | 标记为「不完整 Mock」 |
| 测试文件在功能发布后创建 | 反模式 5 | 标记为「事后补充」 |
| 用例只有「预期行为」无「实际输出」 | 反模式 1 | 标记为「疑似未执行」 |

---

## 五、回归套件架构

### 5.1 三层套件结构

| 套件 | 时长 | 频率 | 覆盖范围 | 通过标准 |
|------|------|------|---------|---------|
| **Smoke** | 15-30 min | 每次提交 | P0 用例（关键路径） | 100% 通过 |
| **定向回归** | 30-60 min | 每次变更 | 受影响模块 + 关联模块 | ≥ 95% 通过 |
| **全量回归** | 2-4 hours | 版本发布前 | 全部用例 | ≥ 90% 通过 |

### 5.2 执行顺序

```
Smoke 先跑 → 失败则停止 → 修复后重跑 Smoke
    ↓ 全过
P0 全过 → 跑 P1 定向回归
    ↓
P1 通过 → 跑 P2 全量回归
    ↓
探索性测试（剩余时间）
```

### 5.3 通过/阻断/有条件通过

| 判定 | 条件 |
|------|------|
| **✅ 通过** | 所有 P0 通过，90%+ P1 通过，无严重缺陷，性能达标 |
| **❌ 阻断** | 任一 P0 失败，发现安全漏洞，数据丢失场景 |
| **⚠️ 有条件通过** | P1 失败但有 workaround，已知问题已记录，修复计划已排期 |

### 5.4 套件维护节奏

| 频率 | 动作 |
|------|------|
| 每月 | 移除过时用例、更新已变更功能、添加新关键路径、优化慢速用例 |
| 每次发布后 | 更新测试数据、修复损坏用例、添加新发现缺陷的回归用例 |
| 每次复测 | 读取上一轮回归套件，更新变更影响的用例 |

---

## 六、来源

| 来源 | 文件 |
|------|------|
| qa-test-planner | `test_case_templates.md`（8 种模板） |
| qa-test-planner | `regression_testing.md`（回归套件架构） |
| test-master | `testing-anti-patterns.md`（5 类反模式） |
| test-master | `qa-methodology.md`（QA 方法论） |
