# Agent 智能体评测方法论参考

> **定位**：本文件整合华为、支付宝（蚂蚁）、字节跳动三家公司的 Agent 评测实践经验，提炼可复用的评测维度、判定标准和执行模式。测试设计师（阶段三）和测试工程师（阶段五）引用本文件，补充 nexus-testing 框架在 AI Agent/Skill 评测方面的方法论。

---

## 一、分层评测架构（三家共识）

三家公司均采用多层评测架构：

| 层级 | 华为 | 支付宝 | 字节 | nexus-testing 对应 |
|------|------|--------|------|-------------------|
| L0 模型选型 | 模型能力基线评测 | 基础能力（QPS/成功率） | 编译通过率 | 阶段二：质量评估 |
| L1 模块评测 | 领域模型过程评测 | 技术维度（RAG/Planning/MCP） | 路径覆盖率 | 阶段五：并行测试 |
| L2 端到端评测 | 商业发布评测 | 业务维度×技术维度交叉分析 | 增量覆盖率 | 阶段七：报告整合 |
| L3 线上评测 | 数据飞轮+线上A/B | 实时RAG效果+在线实验 | 流量蒸馏+采纳率 | 增量复测 |

**nexus-testing 应用**：阶段二质量评估应包含 L0 基线对标，阶段五应覆盖 L1 模块级，阶段七输出 L2 端到端结论。

---

## 二、评测维度矩阵

### 2.1 通用五维评测模型（华为）

| 维度 | 指标 | 适用场景 | 评测方法 |
|------|------|---------|---------|
| **准确性** | 正确性、相关性、完整性、可执行性、流畅度 | 所有 Agent | LLM-as-Judge + 人工抽样 |
| **性能效率** | 并发支持度、流式延迟、非流式延迟 | 需要实时响应的 Agent | 压测工具 + 统计分析 |
| **鲁棒性** | 多轮运行正确性、多轮一致性 | 所有 Agent | 种子集 + 扰动因子库 |
| **一致性** | 多轮回答准确度、多轮回答相同度、上下文一致性 | 对话型 Agent | 上下文注入测试 |
| **安全性** | 内容合规、信息安全、伦理对齐、模型安全 | 所有 Agent | 安全扫描 + 红队测试 |

### 2.2 Skill/Agent 专用评测维度

| 维度 | 指标 | 计算方式 | 通过标准 |
|------|------|---------|---------|
| **触发准确率** | 正确触发 / 总触发次数 | 正向触发 + 逆向触发 | ≥ 95% |
| **工具调用正确率** | 正确调用次数 / 总调用次数 | 参数匹配 + 返回值验证 | ≥ 90% |
| **输出格式合规率** | 合格输出 / 总输出 | 格式校验 + 字段完整性 | 100% |
| **降级容错率** | 正确降级次数 / 总异常次数 | 构造异常场景 | ≥ 80% |
| **Token 效率** | 有效 Token / 总 Token | 输出分析 | ≤ 预估的 150% |

### 2.3 评分公式

**华为 QA Agent 评分**：
```
P = 正确性×0.4 + 相关性×0.3 + 完整性×0.2 + 流畅度×0.1
P ≥ 3.5 为通过（5 分制）
```

**字节单元测试评分**：
```
Case_i = X×时间分 + Y×编译成功率 + Z×覆盖率 + M×断言通过率 + N×有效断言数 + P×有效Mock数 + Q×场景覆盖
Final = Σ(Case_i) / n
```

**nexus-testing 建议评分**（Skill 测试专用）：
```
Score = 功能正确性×0.4 + 安全合规×0.2 + 性能达标×0.2 + 边界覆盖×0.1 + 文档一致×0.1
Score ≥ 3.0 为 Go（5 分制）
```

---

## 三、测试数据管理

### 3.1 种子集 + 扰动生成（华为）

```
选取 10% 准确性测试集作为种子（聚类选取）
    ↓
应用扰动因子库：
  - 语义混淆（同义词替换）
  - 拼写错误/错别字
  - 异常输入（空值/超长/特殊字符）
  - 标点/语气词变化
  - 术语替换
  - 语言风格变化
    ↓
生成鲁棒性测试集（建议占总测试量 20%）
```

**nexus-testing 应用**：阶段三设计测试用例时，从正向用例中选取种子，用扰动因子生成逆向/边界用例。这比手动设计逆向用例更系统化。

### 3.2 合成数据框架（支付宝）

```
模块化算子 + 合成流水线
    ↓
支持：预设步骤（标准化）+ 自定义步骤（行业定制）
    ↓
控制数据分布、质量、多样性
    ↓
事件触发或定时执行（自动化日常合成）
```

### 3.3 真实流量蒸馏（字节）

```
自动化流量 + 业务流量 + 回放流量
    ↓
蒸馏（去重/脱敏/简化）
    ↓
方法覆盖率提升 10-15%
用例通过率提升 30%
流量利用率达 90%
```

**nexus-testing 应用**：`reference-external-case-sourcing.md` 中的外部 case 获取，可以引入「真实流量蒸馏」思路——从生产环境的 Skill 使用日志中提炼测试用例。

---

## 四、判定机制

### 4.1 Agent-as-a-Judge（华为创新）

超越 LLM-as-a-Judge，增加 Agent 能力：

| 能力 | 说明 | 效果 |
|------|------|------|
| **记忆** | 历史标注召回作为 few-shot + 置信度评估 | 减少评分波动 |
| **规划** | 慢思考 CoT（链式推理） | 提升评分准确性 |
| **反思** | 自审评分过程 | 捕捉评分错误 |
| **工具使用** | 算法前置拦截（低级错误直接判定） | 提升效率 |
| **置信度仲裁** | 主Judge + 副Judge 意见不一致时的仲裁策略 | 提升一致性 |

**执行流程**：
```
低级错误拦截（算法检测）
    ↓
历史标注匹配（相似案例查找）
    ↓
主 Judge LLM 评分
    ↓
副 Judge 评分
    ↓
召回率计算
    ↓
置信度仲裁 → 最终分数
```

### 4.2 增强型 Judge Model（支付宝）

在基础 LLM Judge 上叠加：
- 行业知识库检索
- 网络搜索补充
- 行业工具验证
- 专家标注微调

**nexus-testing 应用**：nexus-testing 的 evidence-collector 角色，可以增加「Judge 辅助」能力——用 LLM 对测试结果进行二次判定，而不仅仅依赖 AI agent 的 pass/fail 自报。

### 4.3 自动化验证流水线（字节）

```
编译检查 → 运行检查 → 覆盖率检查 → 断言检查
    ↓
5 步修复循环：
  1. 编译修复（语法错误）
  2. 语法修复（格式问题）
  3. 运行时修复（执行错误）
  4. 断言修复（逻辑错误）
  5. 检测完成
```

**nexus-testing 应用**：对应框架中的「阶段五→阶段六打回机制」。字节的 5 步修复循环可以作为打回时的具体修复指导。

---

## 五、通过/阻断标准（三家汇总）

### 5.1 发布门禁

| 检查项 | 华为 | 支付宝 | 字节 | nexus-testing 建议 |
|--------|------|--------|------|-------------------|
| P0 用例通过率 | 100% | 100% | 100% | 100%（已有） |
| P1 用例通过率 | ≥ 95% | ≥ 90% | ≥ 88% | ≥ 90%（已有） |
| 执行率 | — | — | — | ≥ 90%（已有） |
| 安全扫描 | 全通过 | 全通过 | 全通过 | BLOCKED=0（已有） |
| 性能基线 | 有阈值 | 有阈值 | 有阈值 | ≤ 预估 150%（已有） |
| 鲁棒性 | 多轮一致 | — | — | **建议新增** |

### 5.2 建议新增的门禁项

| 检查项 | 说明 | 阈值建议 |
|--------|------|---------|
| **鲁棒性通过率** | 用扰动因子生成的用例通过率 | ≥ 80% |
| **多轮一致性** | 同一输入多次执行结果一致性 | ≥ 90% |
| **降级容错率** | 异常场景下正确降级的比例 | ≥ 80% |

---

## 六、nexus-testing 框架增强建议

### 6.1 阶段二（质量评估）增强

在现有评估基础上，增加 Agent 专用评估维度：

```
现有：文档质量 / Agent 执行流畅度 / 内容缺失 / 多渠道适配 / Token / 安全
新增：触发准确率预估 / 工具调用复杂度 / 降级路径完备性 / 多轮一致性预估
```

### 6.2 阶段三（测试设计）增强

在现有用例设计基础上，增加：

**种子集扰动生成**：
```
从正向用例中选取种子（每个核心功能模块 ≥ 1 个种子）
    ↓
应用扰动因子库（6 类扰动，见上文 3.1）
    ↓
自动生成逆向/边界用例
    ↓
标注为「扰动生成」+ 原始种子 TC 编号
```

**多轮一致性用例**：
```
TC-XX-M1：第 1 次执行 → 记录输出
TC-XX-M2：第 2 次执行（相同输入）→ 记录输出
TC-XX-M3：第 3 次执行（相同输入）→ 记录输出
判定：3 次输出核心结论一致 → ✅ / 不一致 → ❌（记录差异）
```

### 6.3 阶段五（测试执行）增强

在 sandbox-exec 基础上，增加：

**Judge 辅助判定**：
```
sandbox-exec 执行命令 → 获得实际输出
    ↓
将「预期输出」+「实际输出」+「判定标准」提交给 Judge
    ↓
Judge 输出：✅ 通过（置信度 X%）/ ❌ 失败（原因）/ ⚠️ 需人工复核
```

### 6.4 阶段七（报告整合）增强

在现有 FINAL-TEST-REPORT 基础上，增加：

```
## Agent 评测维度分析

### 准确性
• 功能正确性：X/Y（Z%）
• 输出相关性：X/Y（Z%）
• 输出完整性：X/Y（Z%）

### 鲁棒性
• 扰动用例通过率：X/Y（Z%）
• 多轮一致性：X/Y（Z%）

### 性能效率
• 平均响应延迟：Xms
• Token 效率：实际/预估 = X/Y
```

---

## 七、来源与参考

| 来源 | 演讲者 | 会议 |
|------|--------|------|
| 华为应用开发Agent自动化评测实践 | 胡应广 | QECon 2025 北京 |
| 支付宝行业智能体评测的探索与实践 | 付宇 | QECon 2025 北京 |
| 基于Agent架构的单元测试落地实践 | 赵亮（字节） | QECon 2025 北京 |
| 智能体智能化评测体系 | 符敬伟 | QECon 2025 北京 |
| 用监控打磨Autonomous Agent | 黄闻欣 | QECon 2025 北京 |
