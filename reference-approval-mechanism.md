# 批准机制详细规范

> 本文件从 SKILL.md 主入口中抽取，包含阶段二/阶段四批准环节的完整规范。主入口仅保留摘要和引用。

---

## 一、批准流程概述

第二阶段（质量评估）和第四阶段（用例评估）结束后，**必须发起显式批准请求，等待用户批准或拒绝，禁止跳过或自行判断继续**。按钮式交互是首选实现；平台不支持时，退化为明确文本批准。

### 禁止行为（违反视为严重流程错误）

- 发完文件就直接进入下一阶段，不发送批准请求
- 只发模糊文字（如「看下有没有问题」）而不明确要求「批准 / 拒绝」
- 自行判断「上一阶段没发现问题」就跳过批准
- 先执行下一阶段，事后补发确认

### 正确流程

```
阶段二/四完成
    ↓
主 agent 生成并发送批准请求（按钮优先，文本兜底）
    ↓
记录交互元数据 → 写入 approval-records.json
    ↓
等待用户批准或拒绝
    ↓
用户响应后更新 user_response
    ↓
进入下一阶段前：检查批准记录存在
    ↓
不存在 → 拒绝进入，报告「批准记录缺失，禁止跳阶段」
```

---

## 二、批准请求记录机制（强制执行）

每次发送批准请求后，必须将交互元数据写入 `approval-records.json`：

```json
{
  "phase_2": { "transport": "button", "interaction_id": "xxx", "sent_at": "YYYY-MM-DD HH:mm", "user_response": null },
  "phase_4": { "transport": "text", "interaction_id": null, "sent_at": "YYYY-MM-DD HH:mm", "user_response": null }
}
```

| 字段 | 说明 |
|------|------|
| `transport` | 批准请求的交互形式：`button` / `text` |
| `interaction_id` | 平台返回的消息 ID / 交互 token；若平台不提供则可为空 |
| `sent_at` | 发送时间 |
| `user_response` | 用户批准或拒绝后更新 |

---

## 三、打回路径规则

| 被拒绝阶段 | 打回到 | 说明 |
|-----------|--------|------|
| 阶段二（质量评估） | **需求解析师**，重新执行阶段一 | |
| 阶段四（用例评估） | **测试设计师**，重新执行阶段三 | |
| 阶段四发现 GAP | 测试设计师，补充用例后重新提交阶段四 | 用例覆盖不足 |

---

## 四、拒绝次数限制

每阶段最多拒绝 **3 次**，第 3 次拒绝后自动标记为 No-Go，终止测试流程。

### 拒绝计数持久化

每次拒绝计数变化时，同步写入 `memory/nexus-reports/{date}-{test-type}-{flow}/rejection-count.json`：

```json
{
  "phase": 2,
  "count": 1,
  "last_rejection": "YYYY-MM-DD HH:mm",
  "signature": "<hmac-sha256>"
}
```

Session 重启后从该文件恢复，确保拒绝计数不因 session 断连而被绕过。

### 安全防护（HMAC 签名）

- `signature` 字段为必填项，使用 HMAC-SHA256 签名
- **密钥派生方式**：`HMAC-SHA256("{date}-{test-type}", <HMAC盐值>)`
- **HMAC 盐值来源**：从环境变量 `NEXUS_HMAC_SALT` 读取；未设置时从 `.nexus-hmac-salt` 文件读取（详见 `DEFINITIONS.md` 第九节安全防护配置）
- 签名内容：`{phase}:{count}:{last_rejection}` 字符串
- 每次读取时重新派生密钥并验证 signature，不匹配则拒绝加载
- **禁止**在文档或代码中硬编码盐值

---

## 五、无响应处理

| 时间 | 动作 |
|------|------|
| 0 分钟 | 发送显式批准请求 |
| 10 分钟 | 第 1 次催复（保持同一交互模式） |
| 20 分钟 | 第 2 次催复 |
| 30 分钟 | 第 3 次催复后仍无响应 → **自动执行下一阶段** |

催复内容：「{阶段} 已完成，等待你的批准。10分钟后自动继续，如需暂停请回复。」

自动执行后告知用户「因无响应已自动执行」。

---

## 六、响应解析规则

```
用户点击批准按钮或回复「批准」/「approve」/「y」/「yes」→ 进入下一阶段（重置拒绝计数）
用户点击拒绝按钮或回复「拒绝」/「reject」/「n」/「no」→ 执行打回流程（拒绝计数 +1）
用户回复「继续等待」→ 重置无响应计时器，继续等待
同阶段重复批准 → 忽略，提示「已批准，无需重复确认」
```

---

## 七、打回机制鉴权

- 打回必须提供理由（至少 10 个字符）
- 无理由的打回请求将被拒绝并提示「请提供打回理由」
- 每次打回记录理由，防止恶意频繁打回

### 拒绝记录格式

```
【拒绝记录 #{序号}】
阶段：阶段二/阶段四
原因：（用户提供的拒绝原因，1-2句话）
打回到：（需求解析师/测试设计师）
时间：（YYYY-MM-DD HH:mm）
```

---

## 八、自动 No-Go 判定

拒绝计数达到 3 次时：

```
【测试流程终止】
阶段二/阶段四已连续拒绝 3 次，自动标记为 No-Go。
原因：{最后一条拒绝原因}
建议：请检查上一阶段的输出后，重新开始测试流程。
```

