# Docs

This directory is the home for project documentation, including review records,
execution plans, and the canonical reference set under `docs/references/`.

Active flow and role definitions still remain at the repository root while the
runtime and validator path contracts continue to converge.


