---
name: nexus-testing
description: Nexus AI 测试体系入口。按测试类型自动分流，支持 Skill 测试、网页+接口测试、安卓测试、MCP 测试四种流程。从需求解析到最终报告，全流程覆盖。
---
# Nexus Testing Orchestrator

测试体系主入口。根据用户提供的待测内容，自动识别测试类型并调度对应流程。

## 测试类型识别

当用户请求测试时，系统自动识别：

| 关键词 | 测试类型 | Flow | 流程文件 |
|--------|----------|------|----------|
| Skill / skill | Skill 测试 | Flow A | `flows/skill-testing.md` |
| APK / 安卓 / android | 安卓测试 | Flow C | `flows/android-testing.md` |
| MCP / mcp | MCP 测试 | Flow D | `flows/mcp-testing.md` |
| 网页 / 页面 / web / 接口 / API | 网页+接口测试 | Flow B | `flows/web-api-testing.md` |

**Flow 命名说明**：
- Flow A = Skill 测试（2 个并行测试工程师：skill-tester + security-tester）
- Flow B = 网页+接口测试（5 个并行测试工程师）
- Flow C = 安卓测试（5 个并行测试工程师）
- Flow D = MCP 测试（4 个并行测试工程师）

> **所有阶段、角色、输出文件、超时配置均以 `DEFINITIONS.md` 为单一事实源。**

### 多种测试类型同时请求

当用户同时请求多个测试类型时，按以下规则处理：

**检测到多个关键词时**，主动询问用户测试方式：

```
检测到多个测试类型：
• Skill（Flow A）
• APK（Flow C）
• ...

请选择测试方式：
1.【串行】逐个完成六阶段，再进入下一个（适合时间紧张）
2.【并行】同时启动多个测试流程（适合时间充裕）
3.【只测】我只测其中一个（请指定类型）
```

**意图模糊时**（如「帮我测一下这个链接」），尝试自动识别：
- 能识别 → 正常处理
- 无法识别 → 询问用户：「请明确需要测试的类型」

**自动识别逻辑**：
```
收到模糊请求
    ↓
提取关键词：URL、文件路径、Skill名称
    ↓
判断类型：
  ├─ URL + 网页内容 → Web测试
  ├─ URL + API响应 → 接口测试
  ├─ .apk / Android路径 → 安卓测试
  ├─ Skill名称 / SKILL.md → Skill测试
  ├─ MCP相关关键词 → MCP测试
  └─ 多个关键词混合 → 按权重最高的类型处理
    ↓
无法判断 → 回复：「请明确需要测试的类型，支持：Skill测试 / 网页接口测试 / 安卓APK测试 / MCP测试」
```

## Dual Mode：评审已有测试报告

Nexus 支持两种工作模式，**自动识别**用户意图：

### 模式一：生成模式（默认）

用户要求**创建**测试计划、评审方案、执行测试时，按标准流程执行阶段零~七。

**触发词**：「写测试计划」「生成测试计划」「开始测试」「测试这个 skill」「跑一遍 Flow A」等。

### 模式二：评审模式

用户要求**检查/评审**已有的测试报告或文档时，输出符合性检查结果。

**触发条件**：
- 用户发送 `.md` 文件路径并要求「审一下」「检查」「评审」「review」
- 用户发送文字内容并问「这个测试报告有没有问题」

**评审检查项**（统一引用 `reference-report-format.md`）：

| 检查类别 | 检查内容 | 级别 |
|---------|---------|------|
| 必填节 | 是否含所有必填节（测试范围、策略、用例、追溯、准入准出、环境） | 必改 |
| 缺陷格式 | 缺陷字段是否完整（ID/级别/影响范围/修复建议） | 必改 |
| 占位符 | 是否存在 `[请补充...]` 占位符 | 必改 |
| 命名规范 | 文件名是否使用统一命名（PRODUCT-QUALITY-REVIEW / TEST-CASE-REVIEW 等） | 必改 |
| 阶段完整性 | 各阶段交付物是否齐全 | 建议 |
| 分级一致性 | P0/P1/P2/P3 分级是否连续（无跳跃） | 建议 |
| 追溯完整性 | 需求 ID 与用例 ID 是否对应 | 建议 |

**评审输出格式**：

```
# 评审报告：{文件名}

## 符合性结果

### ✅ 符合项
- [列出符合要求的项]

### ❌ 必改项
1. [问题描述]（位置：章节/行号）
   - 修改建议：[具体建议]

### ⚠️ 建议项
1. [建议描述]
   - 理由：[为什么建议改]

### ⚠️ 可选项
1. [可选优化]
   - 优先级：低

---

## 总体评价
• 必改项：X 个（阻塞）
• 建议项：X 个
• 可选项：X 个

**结论**：[✅ 可接受 / ❌ 需修改必改项后方可使用]
```

**注意**：评审模式不执行阶段零~七，只做静态检查和反馈。

> **占位符规范统一引用** `reference-report-format.md`。

## 阶段零：环境就绪检查（新增）

用户发起测试请求后，**强制执行**环境就绪检查，检查通过才进入阶段一：

```
用户发起测试请求
    ↓
主 agent 自动检测环境就绪状态
    ↓
生成【环境就绪报告】发给用户确认
    ↓
用户确认通过 → 进入阶段一
用户拒绝 → 说明具体问题，等待修复后重新检查
```

**⚠️ 强制执行保证**：阶段零的环境就绪报告发送后，主 agent **必须等待用户确认**，不得跳过或提前进入阶段一。主 agent 在阶段零完成后输出「阶段零完成，等待你的确认」，等待用户明确回复「确认」或「通过」后，才开始阶段一。

**确认词匹配规则（严格）**：
- ✅ 有效确认：「确认」「通过」（完整词语，不区分大小写）
- ❌ 无效确认（作拒绝处理）：单字如「好」「行」「可」「y」「ok」、谐音字、表情符号、与测试无关的文字
- 主 agent 判断为无效确认时，提示「请回复「确认」或「通过」后继续，不接受单字或谐音」

**环境就绪检查项（按 Flow 类型）**：

| Flow | 必检项 |
|------|--------|
| Flow A（Skill） | Skill 源码路径是否存在 / SKILL.md 语法是否可读 / YAML frontmatter 完整性 / **依赖环境检测**（见下方） |
| Flow B（网页/接口） | URL 是否可访问 / API 是否返回有效响应 |
| Flow C（安卓） | APK 文件路径是否存在 / 文件是否可读取 |
| Flow D（MCP） | MCP Server 地址是否可连接 / JSON-RPC 端口是否开放 |

**通用检查项（所有 Flow）**：
- 输出目录 `memory/nexus-reports/` 是否可写
- 渠道配置检查（飞书/QQ/微信）

**Flow A 增强检查项**：
| 检查项 | 说明 |
|--------|------|
| YAML frontmatter 必填字段 | name、description 是否存在 |
| 外部引用路径 | 检测 SKILL.md 中引用的文件路径是否存在 |
| 外部 URL | 检测是否有外部网络资源引用 |

**Flow A 依赖环境检测**（阶段零必检）：

> **前提**：本框架运行在 OpenClaw 环境中，OpenClaw 本身的可用性**不作为检测项**（假定已就绪）。此处仅检测被测 Skill 额外依赖的运行时环境。

| 检查项 | 检测方法 | 状态标记 |
|--------|---------|---------|
| Node.js / npm 环境 | 扫描 Skill 源码及 SKILL.md，若引用 `npm`、`node`、`package.json` 或 JS/TS 工具 → 执行 `node -v` 和 `npm -v` 验证 | ✅ 已安装(版本) / ❌ 未安装 / ⏭️ 不需要 |
| Python 环境 | 扫描 Skill 源码及 SKILL.md，若引用 `python`、`pip`、`requirements.txt` 或 `.py` 文件 → 执行 `python --version` 和 `pip --version` 验证 | ✅ 已安装(版本) / ❌ 未安装 / ⏭️ 不需要 |
| Python 依赖包 | 若存在 `requirements.txt`，检查关键包是否已安装（`pip list` 比对） | ✅ 已安装 / ⚠️ 缺失 X 个包 / ⏭️ 不需要 |
| npm 依赖包 | 若存在 `package.json`，检查 `node_modules` 是否存在或执行 `npm ls --depth=0` | ✅ 已安装 / ⚠️ 缺失依赖 / ⏭️ 不需要 |
| 引用的其他 OpenClaw 插件 | 扫描 SKILL.md 的 tools 字段和源码中的插件调用，提取依赖的外部插件列表 → 验证这些插件是否在当前环境中可用 | ✅ 全部可用 / ⚠️ X 个插件不可用 / ⏭️ 无外部插件依赖 |
| 系统命令依赖 | 扫描源码中的 shell 调用（`exec`、`spawn`、`subprocess` 等），提取依赖的系统命令 → 执行 `which`/`where` 验证 | ✅ 全部可用 / ⚠️ X 个命令不可用 / ⏭️ 无系统命令依赖 |

**依赖检测逻辑**：
```
扫描 SKILL.md + Skill 源码
    ↓
识别依赖类型（npm/python/插件/系统命令）
    ↓
对每种检测到的依赖执行环境验证
    ↓
未检测到该类依赖 → 标记为「⏭️ 不需要」（跳过）
检测到且环境满足 → 标记为「✅」
检测到但环境不满足 → 标记为「❌ / ⚠️」并给出修复建议
    ↓
任一必要依赖标记为 ❌ → 阶段零结论为「❌ 环境不就绪」
仅 ⚠️ 警告 → 阶段零结论附带警告，用户确认后可继续
```

**环境就绪报告格式**：
```
# 环境就绪报告

## Flow A（Skill 测试）

| 检查项 | 状态 | 详情 |
|--------|------|------|
| Skill 源码路径 | ✅ / ❌ | /path/to/skill |
| SKILL.md 语法 | ✅ / ❌ | 可读/语法错误 |
| YAML frontmatter 完整性 | ✅ / ❌ | name/description 存在 |
| 外部引用路径 | ✅ / ❌ | X 个文件存在 |
| 外部 URL | ✅ / ⚠️ | X 个 URL |

## 依赖环境检测
| 检查项 | 状态 | 详情 |
|--------|------|------|
| Node.js / npm | ✅ / ❌ / ⏭️ | v20.11.0 / npm 10.2.0 / 不需要 |
| Python | ✅ / ❌ / ⏭️ | 3.12.1 / 不需要 |
| Python 依赖包 | ✅ / ⚠️ / ⏭️ | 全部已安装 / 缺失: requests, flask |
| npm 依赖包 | ✅ / ⚠️ / ⏭️ | 全部已安装 / 缺失依赖 |
| 引用的 OpenClaw 插件 | ✅ / ⚠️ / ⏭️ | 全部可用 / 不可用: plugin-x |
| 系统命令 | ✅ / ⚠️ / ⏭️ | 全部可用 / 不可用: ffmpeg |

## 渠道配置
| 渠道 | 状态 |
|------|------|
| Telegram | ✅ 已配置 |
| 飞书 | ⚠️ 未配置 |
| QQ | ⚠️ 未配置 |
| 微信 | ⚠️ 未配置 |

结论：✅ 环境就绪 / ❌ 存在 X 个问题，请修复后重新检查
```

## 执行阶段

无论哪种测试类型，都经过以下 8 个标准阶段（阶段零 ~ 阶段七），且支持缺陷打回重测的闭环流程：

```
阶段零：环境就绪检查
阶段一：需求解析 → 需求解析师
阶段二：质量评估 → 质量评估师（评估产品本身）→ 需批准
阶段三：测试设计 → 测试设计师
阶段四：用例评估 → 用例评估师（评估测试用例）→ 需批准
阶段五：并行测试执行 → 各测试工程师（每个角色独立交付物）
       ↓
阶段五后：证据收集 → evidence-collector（所有 subagent 完成后执行）
       ↓
阶段六：缺陷分析 → 缺陷分析师 →【打回机制】
       ├─ 无问题 → 进入阶段七报告整合
       └─ 有问题 → 打回测试设计师/测试工程师重新测试 → 循环阶段五/六（**最多 3 轮**，第 3 轮打回后仍不达标则强制进入阶段七并在报告中标注「测试覆盖不完整」）
阶段七：报告整合 → 报告整合师
```

## 报告输出目录

所有测试报告输出到：`memory/nexus-reports/{date}-{test-type}-{flow}/`

> **目录结构统一引用** `DEFINITIONS.md` 第三节。

**复测时**：旧报告移入 `archive/{timestamp}/` 子目录，不覆盖已有内容。

## 执行模型与角色调用机制

### 混合执行模型

Nexus Testing 采用**混合执行模型**：主 agent 协调 + 并行 subagent 执行。

| 阶段 | 执行模式 | 调用机制 |
|------|----------|---------|
| 阶段一（需求解析） | **主 agent 直接执行** | 读取被测对象，生成 `SPEC.md` |
| 阶段二（质量评估） | **主 agent 直接执行** | 读取 `SPEC.md`，生成评估报告 |
| 阶段三（测试设计） | **主 agent 直接执行** | 读取 `SPEC.md`，生成 `TEST-DESIGN.md` |
| 阶段四（用例评估） | **主 agent 直接执行** | 读取 `TEST-DESIGN.md`，生成评估报告 |
| 阶段五（并行测试） | **subagent 并行执行** | 启动多个独立 subagent，各角色并行执行 |
| 阶段六（缺陷分析） | **主 agent 直接执行** | 汇总阶段五结果，生成 `DEFECTS/DEFECT-REPORT.md` |
| 阶段七（报告整合） | **主 agent 直接执行** | 汇总所有结果，生成最终报告 |

### 阶段五 subagent 并行执行（关键实现）

阶段五是性能关键，各 Flow 的测试工程师角色**必须并行执行**，不等待彼此。

**Flow A（Skill 测试）并行角色**：skill-tester、security-tester（共 2 个，不含 compatibility-tester，Skill 测试使用专门的渠道适配检测）

**Flow D（MCP 测试）并行角色**：mcp-tester、security-tester、performance-tester、reality-checker（共 4 个）

**evidence-collector**：独立角色，非并行组，**在阶段五所有 subagent 全部完成后**才开始执行证据收集

#### 阶段五启动流程

```
阶段四批准通过
    ↓
确认报告目录存在：memory/nexus-reports/{date}-{test-type}-{flow}/
    ↓
启动 subagent（并行），等待所有完成（**最多 15 分钟/900 秒**，统一值）
    ↓
Flow A：skill-tester、security-tester（共 2 个）
Flow B：5 个并行工程师
Flow C：5 个并行工程师
Flow D：mcp-tester、security-tester、performance-tester、reality-checker（共 4 个）
    ↓
所有 subagent 完成 → 进入阶段五后：证据收集
    ↓
evidence-collector 执行（独立步骤，不参与并行）
    ↓
读取各 subagent 输出的结果文件 + evidence-collection.md
    ↓
进入阶段六
```

#### 阶段五 subagent 启动指令

使用**平台支持的并行子任务能力**启动每个角色。Skill 文档只约束“并行启动”和“独立产出”，**不写死具体 API、工具名或 SDK 调用方式**。evidence-collector 不参与并行，在各 subagent 全部完成后独立执行。

超时、重试、Token 阈值统一引用 `DEFINITIONS.md` 第五节、第七节。启动后执行一次轻量检查，确认预期角色已开始写入结果或进度文件。

#### 阶段五 subagent 输入/输出约定

| subagent | 读取文件 | 输出文件 | 备注 |
|----------|---------|---------|------|
| skill-tester | `SPEC.md`、`TEST-DESIGN.md`、Skill 源码 | `TEST-EXECUTION/skill-results.md` | 含渠道适配检测 |
| security-tester | `SPEC.md`、Skill 源码 | `TEST-EXECUTION/security-results.md` | 含工具调用安全检测 |
| evidence-collector | 监听所有测试输出 | `DEFECTS/evidence-collection.md` | 阶段五所有 subagent 完成后执行 |
| compatibility-tester | `SPEC.md`、框架输出样本 | `TEST-EXECUTION/compatibility-results.md` | **Flow B/C/D 专用，Flow A 不使用** |

### 上下文传递（文件系统）

所有阶段间的上下文通过文件系统传递，**无需通过工具参数传递**。

> 引用 DEFINITIONS.md 第二节的统一上下文传递路径。

```
阶段一 → SPEC.md → 阶段二读取
阶段二 → PRODUCT-QUALITY-REVIEW.md → 阶段三读取（参考）
阶段三 → TEST-DESIGN.md → 阶段四读取
阶段四 → TEST-CASE-REVIEW.md → 阶段五各 subagent 读取（各自读取需要的文件）
阶段五 → TEST-EXECUTION/*.md → 阶段六汇总
```

### 阶段间错误处理

| 错误场景 | 处理策略 |
|----------|---------|
| subagent 启动失败 | 按 `DEFINITIONS.md` 的统一重试规则处理；重试后仍失败则主 agent 汇总为 blocker，并决定是否向用户请求跳过该角色 |
| 阶段五某个 subagent 超时/失败 | 保留已产出文件，明确记录完成度、未覆盖范围和建议方案；是否跳过由主 agent 决策并对用户说明 |
| 阶段五 Token 预警 | 按 `DEFINITIONS.md` 第七节执行；由主 agent 统一对外沟通，subagent 只写状态与部分结果 |

**Phase 5 → Phase 6 联动规则**（必须执行）：
- 阶段五任一角色跳过、失败或仅部分执行 → 阶段六必须记录为“该维度未完整覆盖”
- Go/No-Go 判定按维度覆盖情况输出，不因单个角色失败而掩盖其他已完成结果
- 最终报告必须明确说明未覆盖维度、原因和残余风险

### 阶段五进度可见性（强制）

阶段五每个 subagent **完成时**写入进度结果到文件（而非定时轮询），主 agent 汇总展示给用户。

**进度文件**：`memory/nexus-reports/{date}-{test-type}-{flow}/TEST-EXECUTION/progress-{角色}.txt`

**进度文件格式**：
```
{角色} 进度报告
时间：HH:mm:ss
完成度：XX%
当前状态：{正在执行什么}
发现缺陷：X 个
```

**主 agent 汇总展示**（每个 subagent 完成时汇总一次）：
```
阶段五进行中：
• skill-tester：60%（正在扫描YAML）
• compatibility-tester：30%（正在执行兼容性矩阵）
• security-tester：0%（等待中）
预计剩余：约5分钟
```

### 阶段五启动确认（一次性检查）

启动后执行一次，用于确认 subagent 是否真正启动成功：

```
阶段五 subagent 全部启动后：
    ↓
等待每个 subagent 首次写入文件（检查 TEST-EXECUTION/ 目录）
    ↓
10 秒内任一 subagent 未写入 → 发出预警
    ↓
30 秒内仍未写入 → 判断为启动失败，执行重试（最多3次）
    ↓
全部 subagent 均写入文件 → 确认「启动成功」
```

**注意**：这是启动时的一次性检查，不是运行时超时处理。运行时超时由「阶段间错误处理」表中的规则处理。

### 阶段五运行时超时处理

运行时超时（subagent 运行中超过 15 分钟）由错误处理表规则处理，不属于启动确认范围。主 agent 自身调用失败时，同样按统一重试和 blocker 升级规则处理。

### 阶段五并行性保障

阶段五的关键是**真正的并行**，不串行等待：

- 所有 subagent **同时启动**，不等一个完成再启动下一个
- 每个 subagent 独立执行、独立计时（**最多 15 分钟/900 秒**，统一值）
- 任一 subagent 完成即写入结果，不等其他 subagent
- 最终汇总时，缺失的 subagent 结果标记为「未执行」

## 当前会话信息

- **平台**：OpenClaw
- **工作目录**：由运行时注入，**不要在 Skill 中写死本地绝对路径**
- **测试报告目录**：`memory/nexus-reports/{date}-{test-type}-{flow}/`

**支持的渠道**：Telegram、飞书、QQ、微信。**其他渠道暂不支持**，如需使用请手动配置。

**辅助角色触发条件**：
- tool-evaluator：用户主动发送「评估工具」时触发
- workflow-optimizer：检测到流程有优化空间时触发

---

## 沟通协议（必须遵守）

### 测试开始

```
【产品名称】开始测试
预期流程分为8个阶段（阶段零~七）
阶段零：环境就绪检查（自动执行）
阶段一：需求解析（需求解析师）
阶段二：质量评估（质量评估师）→ 需批准
阶段三：测试设计（测试设计师）
阶段四：用例评估（用例评估师）→ 需批准
阶段五：并行测试执行（各测试工程师）
阶段六：缺陷分析（缺陷分析师）
阶段七：报告整合（报告整合师）
```

### 阶段进入

```
现在开始进行第x阶段
参与角色：xxx
```

**确认与推进规则**：
- 阶段零：必须等待用户明确回复「确认」或「通过」
- 阶段二、阶段四：必须等待批准
- 其他阶段：前置交付物齐备后自动推进；只有缺少输入或出现 blocker 时才向用户提问

**⚠️ 主 agent 必须确认清单（每个阶段开始前必查）：**
- [ ] 上一阶段的交付物已生成并已发送给用户
- [ ] 下一阶段的角色 subagent 已启动（阶段三/四：主 agent 直接执行则跳过）
- [ ] 阶段五并行：对应 Flow 的 subagent 必须同时启动，不可遗漏
- [ ] 本阶段所需的确认或批准已完成

**⚠️ 强制执行保证**：
- 阶段二和阶段四必须发起显式批准请求
- 平台支持按钮时优先使用按钮；不支持时退化为明确文本批准
- 在收到批准之前，禁止进入下一阶段

### 阶段完成

```
第x阶段完成
参与角色：xxx
交付物：[文件名]（立即发送）
发现缺陷：X个（P0:X / P1:X / P2:X / P3:X）
```

### 文件发送规范（强制执行）

**发送文件时，优先使用平台支持的“文件 + 摘要说明”同次发送能力。** 如果平台不支持 caption，同一轮输出中也必须紧跟摘要说明，禁止只发文件不解释。

**摘要说明格式要求**：
- 文件名
- 关键内容摘要（1-3句话）
- 包含什么
- 请查收
- 下一步是什么

### 批准环节（强制执行）

第二阶段（质量评估）和第四阶段（用例评估）结束后，**必须发起显式批准，等待用户批准或拒绝，禁止跳过或自行判断继续**。按钮式交互是首选实现；平台不支持时，退化为明确文本批准。

> **完整的批准机制规范（交互记录、HMAC 签名、打回路径、拒绝计数持久化、无响应处理、No-Go 判定）统一引用** `reference-approval-mechanism.md`。

**核心规则速查**：
- 每阶段最多拒绝 **3 次**，第 3 次后自动 No-Go 终止
- 打回路径：阶段二→阶段一，阶段四→阶段三
- 打回必须提供理由（≥10 字符）
- 无响应 30 分钟后自动继续
- 平台支持按钮时优先使用按钮；若平台返回消息 ID 或交互 token，必须写入记录

**阶段门禁执行顺序（硬性约束，违反视为严重流程错误）**：

```
阶段 N 完成 → 发送阶段 N 交付物 + 批准请求（如需） → 立即停止，等待批准
    ↓
收到批准 → 才开始执行阶段 N+1 → 生成阶段 N+1 交付物
    ↓
发送阶段 N+1 交付物 + 批准请求（如需） → 立即停止
```

1. 阶段 N 完成后，**只发送阶段 N 的交付物**，不执行也不输出阶段 N+1 的任何内容
2. 如阶段 N 需批准（阶段二、阶段四），发送交付物 + 批准请求后 **立即停止**，等待批准
3. 批准请求 **只发一次**，禁止重复发送
4. 收到批准后，**才开始** 执行下一阶段并生成其交付物
5. 禁止将两个阶段的输出合并在同一条消息中发送

> 门禁详细规范引用 `DEFINITIONS.md` 第九节「阶段门禁强制规则」。

### 需要用户提供信息时

```
需要你提供：
1.【优先级】[描述]
2.【优先级】[描述]

用户提供完整信息后，自动继续下一阶段。
```

---

## 参考文档索引

> 以下文档为主入口的详细补充，按需查阅：

| 文件 | 内容 | 何时查阅 |
|------|------|---------|
| `DEFINITIONS.md` | 阶段/角色/文件/超时/Token/渠道的单一事实源 | 任何定义有疑问时 |
| `reference-report-format.md` | 报告格式与占位符统一规范 | 写报告/评审报告时 |
| `reference-security-blacklist.md` | 摘要说明 / Caption XSS 注入字符黑名单 | 发送文件或校验摘要说明时 |
| `reference-security-scan.md` | 六阶段安全扫描（提示词注入/恶意代码/凭证泄露/结构命令/供应链/权限审计）+ BLOCKED/REVIEW/SAFE 判定体系 + 外部工具集成 | Skill 安装前安全门禁、安全测试时 |
| `reference-approval-mechanism.md` | 批准/拒绝/HMAC/打回/No-Go 完整规范 | 阶段二/四批准时 |
| `reference-flow-skill.md` | Flow A（Skill 测试）详细用例模板 | 执行 Skill 测试时 |
| `reference-flow-web-api.md` | Flow B（网页+接口）详细用例模板 | 执行网页/接口测试时 |
| `reference-flow-android.md` | Flow C（安卓）详细用例模板 | 执行安卓测试时 |
| `reference-flow-mcp.md` | Flow D（MCP）详细用例模板 | 执行 MCP 测试时 |

---

## 执行原则

1. **交付物必须附带摘要说明**：优先使用 caption；平台不支持时，必须紧跟说明文本
2. 每个阶段完成后，交付物立即发送
3. 第二、第四阶段结束后必须等待用户批准，不擅自继续
4. 发现缺陷立即记录，截图存档
5. 阻塞性问题（P0/P1）立即上报
6. 保持环境纯净，测试结束后清理临时文件
7. 仅阶段零需要确认，阶段二/四需要批准；其他阶段按流程自动推进

### 8. 【最高优先级】真实执行原则（反偷懒机制）

> **核心规则：读文档 ≠ 测试。静态分析 ≠ 验证。查看配置 ≠ 执行。**

**阶段五每个测试用例必须包含执行证明**，格式如下：

```
#### TC-XX：（用例名称）
• 执行动作：（具体执行了什么命令/操作/调用）
• 实际输入：（真实传入的参数/数据）
• 实际输出：（真实返回的内容/报错/截图，非预期结果）
• 判定：✅ 通过 / ❌ 失败
```

**禁止的偷懒行为**（违反视为测试无效，必须重做）：

| 偷懒行为 | 正确做法 |
|---------|---------|
| 「读取 SKILL.md，验证逻辑完整」 | 安装 Skill → 真实触发 → 观察实际行为 |
| 「检查渠道配置，确认已配置」 | 实际发送一条测试消息 → 验证送达 |
| 「环境不足，跳过执行」 | 尝试降级方案（沙箱/mock/手动构造）→ 记录尝试过程 |
| 「验证命令参数格式正确」 | 实际执行命令 → 检查真实输出 |
| 用例结果只有「预期行为」无「实际输出」 | 每条用例必须写出真实的执行输出或返回值 |

**执行降级阶梯**（环境不足时按优先级尝试，不得直接跳过）：
```
1. 真实执行（最优）→ 在真实环境中运行
2. 沙箱执行 → 使用受限环境运行
3. 构造模拟 → 手动构造输入/输出模拟执行路径
4. 部分执行 → 执行可执行的部分，明确标注未执行部分
5. 降级为静态分析（最后手段）→ 必须标注为「P1 未执行，仅静态分析」
```

**执行率硬性要求**（完整定义见 `DEFINITIONS.md` 第十节）：
- 执行率 = 实际执行的用例数 / 设计的总用例数
- 执行率 ≥ 90% → 正常通过
- 执行率 70%~89% → 测试报告必须标注「测试覆盖不足」
- 执行率 50%~69% → 强制打回，不允许直接结束流程
- 执行率 < 50% → 测试报告标记 No-Go
9. **摘要说明强制校验**：主 agent 在发送任何文件前，验证摘要说明包含五要素（文件名 + 内容摘要 + 包含什么 + 请查收 + 下一步），缺失任一要素则拒绝发送；并执行内容过滤：
   - 最大长度：500 字符
   - **XSS 注入字符黑名单**：统一引用 `reference-security-blacklist.md`（检测到任一项 → 拒绝发送）
   - 超过限制 → 截断并标注 `[内容截断]`
   - 检测到注入字符 → 拒绝发送，提示用户
10. **微信渠道降级**：统一引用 `DEFINITIONS.md` 第八节「渠道降级规则」。**禁止在此处重复手写渠道降级逻辑。**
11. **阻塞升级由主 agent 统一处理**：遇到 Token 预警、超时、环境不足等问题时，subagent 先保留已产出结果和 blocker，再由主 agent 决定是否跳过、降级或继续

